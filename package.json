{
  "name": "mvc-usuarios-react",
  "version": "1.0.0",
  "description": "Sistema MVC de Usuarios con React y SonarQube",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "test:all": "npm run test:backend && npm run test:frontend",
    "coverage:report": "node scripts/coverage-report.js",
    "sonar:backend": "cd backend && npm run sonar",
    "sonar:frontend": "cd frontend && npm run sonar",
    "sonar:all": "npm run test:all && npm run coverage:report && npm run sonar:backend && npm run sonar:frontend",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start:sonar": "docker-compose -f docker-compose.sonar.yml up -d",
    "stop:sonar": "docker-compose -f docker-compose.sonar.yml down",
    "logs:sonar": "docker-compose -f docker-compose.sonar.yml logs -f",
    "test": "npm run test:backend && npm run test:frontend"
  },
  "keywords": [
    "mvc",
    "react",
    "nodejs",
    "express",
    "sonarqube",
    "testing"
  ],
  "author": "Tu Nombre",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}